import { useLocation } from "react-router-dom";

const Breadcrumbs = () => {
    const currentLocation = useLocation();

    return (
        <div>
            Breadcrumbs
        </div>
    );
};

export default Breadcrumbs;